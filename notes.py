# Primary key - a number that uniquely identifies one record in a table
# Foreign key  - a number from another table that is used to link tables together.
# Join         - combining rows from two or more tables into one result set.
# One to many -  when one parent can have multiple children, but each child only has one parent.
# Many to many - when both parents can have multiple children and each child can belong to multiple parents.

# when we have two tables, artwork and museum. Each artwork belongs to a museum with each museum having many artworks.
# in a one to many relationship the many recieves the foreign key.